import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.suiteBanesco.app',
  appName: 'Banesco Pagos',
  webDir: 'dist/pagos-app',
  bundledWebRuntime: false
};

export default config;
